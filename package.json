{
  "name": "clinmedkaz",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "sh -c 'npm run dev:server & npm run dev:client; wait'",
    "dev:server": "npm run develop --prefix server",
    "dev:client": "npm run dev --prefix client",
    "build": "npm run build:server && npm run build:client",
    "build:client": "npm run build --prefix client",
    "build:server": "npm run build --prefix server",
    "test": "npm run test --prefix server && npm run build:client",
    "start:server": "npm run start --prefix server",
    "start": "npm run start:server",
    "dev:cms": "npm run develop --prefix server",
    "start:cms": "npm run start --prefix server",
    "build:cms": "npm run build --prefix server"
  },
  "engines": {
    "node": ">=22.13.0 <=26.x.x"
  }
}
